import { HttpBase } from './http-base.client';
import { StorageZoneFile } from '../models';
export declare class StorageClient extends HttpBase {
    constructor(httpClient?: any);
    get(storageZone: string): Promise<StorageZoneFile | StorageZoneFile[]>;
    getFile(filePath: string): Promise<string>;
    update(filePath: string, fileContents: string): Promise<string>;
    delete(filePath: string): Promise<string>;
}
