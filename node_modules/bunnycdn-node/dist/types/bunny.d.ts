import 'reflect-metadata';
import { HttpBase, PullZoneClient, StorageClient } from './clients';
import { Billing, Statistic } from './models';
export declare class Bunny extends HttpBase {
    private _pullZoneClient;
    readonly pullZone: PullZoneClient;
    private _storageClient;
    readonly storage: StorageClient;
    constructor(httpClient?: any);
    statistics(): Promise<Statistic[]>;
    billing(): Promise<Billing[]>;
    applyCode(couponCode: string): Promise<void>;
    purge(url: string): Promise<void>;
    hardUpdate(host: string, storageZone: string, path: string, fileName: string, fileContents: string): Promise<void>;
}
